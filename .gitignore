# Rust / Zed Extension
target/
*.so
*.dylib
*.dll

# Tree-sitter grammar (cloned by Zed during dev extension install)
grammars/http/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# Compiled test binaries
lsp/test_parser
lsp/test_parser2

# Test artifacts
tests/fixtures/temp/
.http-responses/

# Private environment config (contains secrets — any directory)
**/http-client.private.env.json

# Persisted response variables and local state (any directory)
**/.http-client/.global-variables.json
**/.http-client/active-environment
**/.http-client/environments.json
**/.http-client/history/

# Node.js (for grammar development)
node_modules/
package-lock.json

# Python virtual environments
venv/
.venv/
*.pyc

# Logs
*.log

# Editor backups
*.bak
*.backup
*.orig
*.tmp

# Zed local settings
.zed/

# Grammar backups
grammars/*.backup

# Video source files (keep only .gif for README)
*.mov
*.mp4

# Tree-sitter generated C sources
src/grammar.json
src/node-types.json
src/parser.c
src/tree_sitter/

# Built extension (generated during build)
extension.wasm
